<%= simple_form_for([@scooter, @reservation]) do |f| %>
<%= f.input :start_date %>
<%= f.input :end_date %>
<%= f.button :submit, class: "btn btn-primary" %>
<% end %>

<!-- have this on the show page -->
